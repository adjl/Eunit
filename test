#!/u/u1/masrjb/bin/euscheme -fq

(load "unit-test.lisp")

(defun run-tests (tests)
  (cond (tests (load (car tests))
               (if (cdr tests) (newline))
               (run-tests (cdr tests)))))

(run-tests
  '("test-unit-test.lisp"))
