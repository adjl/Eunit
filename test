#!/u/u1/masrjb/bin/euscheme -fq

(load "unit-test.lisp")

(defun run-tests (tests)
  (cond ((not (null tests)) (load (car tests))
                            (if (not (null (cdr tests))) (newline))
                            (run-tests (cdr tests)))))

(run-tests
  '("test-unit-test.lisp"))
